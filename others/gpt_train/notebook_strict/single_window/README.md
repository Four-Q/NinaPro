# 单窗口独立实验

本目录中的模型输出只允许依赖当前样本的 `X: [16, 40]`。训练前会比较同一
样本在单独推理、普通组批和 batch 换位三种情况下的 logits；误差超过
`1e-5` 时拒绝训练。

禁止使用相邻窗口、batch 内聚合、测试顺序、Vin、文件名或其他元数据。
数据集、DataLoader、损失函数和 `src.training.fit` 与原 notebook 保持一致。
